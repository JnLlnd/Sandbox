#SingleInstance force

Gui, New, +Resize -MaximizeBox +MinSize560x505 +MaxSizex505 +Hwndg_strGui2Hwnd, Title

instanceA := new A("Bob")

Gui, Show

return

class A {
	__New(name)
	{
		this.name := name
		Gui, Add, Edit, gthis.Changed(), % this.name
	}
	
	Changed()
	{
		###_V("this.name", this.name)
	}
}
